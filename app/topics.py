# Rename a topic and change it in one place for every service!

IMAGE_SUBMITTED     = "image.submitted"       # Upload Service → OCR Service
INFERENCE_COMPLETED = "inference.completed"   # OCR Service → Document DB Service
ANNOTATION_STORED   = "annotation.stored"     # Document DB Service → Embedding Service (Week 2)
EMBEDDING_CREATED   = "embedding.created"     # Embedding Service → Vector Index (Week 2)